info:
  version: 1.0.0
  description: L25GC-plus UPF-U dataplane configuration

configuration:
  dataplane:
    dn_mac: "3c:fd:fe:b3:13:cc"
    an_mac: "3c:fd:fe:b0:f0:b5"
    upf_ip: "192.168.1.2"

    ports:
      access: 1                     # ACCESS_PORT
      core: 0                       # CORE_PORT (SGi follows CORE)