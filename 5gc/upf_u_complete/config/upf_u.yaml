info:
  version: 1.0.0
  description: L25GC-plus UPF-U dataplane configuration

configuration:
  dataplane:
    dn_mac: "90:e2:ba:b3:bb:7c"
    an_mac: "90:e2:ba:b3:b9:51"
    upf_ip: "192.168.1.2"

    ports:
      access: 1                     # ACCESS_PORT
      core: 0                       # CORE_PORT (SGi follows CORE)
